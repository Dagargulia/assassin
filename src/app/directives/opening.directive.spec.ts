import { OpeningDirective } from './opening.directive';

describe('OpeningDirective', () => {
  it('should create an instance', () => {
    const directive = new OpeningDirective();
    expect(directive).toBeTruthy();
  });
});
